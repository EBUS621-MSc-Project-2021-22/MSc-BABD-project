library(igraph)
library(igraphdata)

set.seed(8675309)

gl <- graph(edges = c(1,2,
                      1,3,
                      2,4,
                      3,5,
                      5,1,
                      4,3))
summary(gl)
plot(gl)
